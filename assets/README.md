# Assets Directory

Place additional assets here:

- `fourier_portrait.svg` - Portrait of Joseph Fourier (SVG format)
- Any additional images for the timeline or other sections

The scripts will automatically use `fourier_portrait.svg` if available, otherwise they will display a placeholder.
